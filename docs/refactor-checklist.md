# 改造清单（车卡器）

## P0（已完成）

- [x] 导出阶段对模板公式单元格启用“公式优先”策略：检测到公式即跳过写入。
- [x] 导出时仅记录跳过的公式单元格，不再做代码值对比。
- [x] 幸运值增加回填路径：当 `V10` 为公式单元格时，改写 `T7` 作为输入位。
- [x] 草稿自动持久化：本地草稿自动保存，刷新后可继续编辑。
- [x] 重置时清理草稿：在“重新捏人与清空”操作中清除本地草稿。

## P1（建议下一步）

- [ ] 把导出映射集中为单一配置源（属性、技能、背景、武器），减少 `Stage4Export` 与工具脚本重复维护。
- [ ] 将公式保护名单固化到 `tools/check-export.js`，把关键公式保护从“代码约定”升级为“自动检查”。
- [ ] 为导出增加“调试报告下载”选项（记录跳过的公式格、对比结果、写入统计）。
- [ ] 为草稿增加版本号迁移器（处理字段新增/改名时的兼容恢复）。

## P2（性能与体验）

- [ ] Excel XML 写入增加地址索引缓存（row/cell map），减少大批量 `setWorksheetCell` 的扫描成本。
- [ ] 数据按需加载：职业列表拆分为摘要/详情，降低首屏 JSON 体积。
- [ ] 职业/武器搜索增加输入防抖与结果缓存，优化低端机滚动与筛选流畅度。
- [ ] 导出过程可选 Worker 化，避免主线程长任务造成交互卡顿。

## P3（工程质量）

- [ ] 增加导出回归测试：固定角色样本 -> 导出 -> 校验关键单元格与公式保留。
- [ ] 增加规则单测：职业解析、信用评级区间、年龄修正边界。
- [ ] 清理历史 `app.js` 单体实现，避免与 `src/` 版本并存造成维护歧义。
